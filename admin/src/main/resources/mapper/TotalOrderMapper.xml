<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mc.app.repository.TotalOrderRepository">

    <select id="select" resultType="TotalOrder">
        SELECT * FROM total_order
    </select>

    <select id="selectOne" parameterType="int" resultType="TotalOrder">
        SELECT * FROM total_order WHERE order_key = #{orderKey}
    </select>

    <insert id="insert" parameterType="TotalOrder">
        INSERT INTO total_order
        (cust_id, addr_key, item_key, order_date, recipient_name, recipient_phone,
         order_req, order_status, order_card, order_total_price, order_shoot_num)
        VALUES
            (#{custId}, #{addrKey}, #{itemKey}, #{orderDate}, #{recipientName}, #{recipientPhone},
             #{orderReq}, #{orderStatus}, #{orderCard}, #{orderTotalPrice}, #{orderShootNum})
    </insert>

    <update id="update" parameterType="TotalOrder">
        UPDATE total_order SET
                               cust_id = #{custId},
                               addr_key = #{addrKey},
                               item_key = #{itemKey},
                               order_date = #{orderDate},
                               recipient_name = #{recipientName},
                               recipient_phone = #{recipientPhone},
                               order_req = #{orderReq},
                               order_status = #{orderStatus},
                               order_card = #{orderCard},
                               order_total_price = #{orderTotalPrice},
                               order_shoot_num = #{orderShootNum}
        WHERE order_key = #{orderKey}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM total_order WHERE order_key = #{orderKey}
    </delete>

    <select id="selectOrderCount" resultType="int">
        SELECT COUNT(*) FROM total_order
    </select>

    <select id="selectTodayRevenue" resultType="int">
        SELECT IFNULL(SUM(order_total_price), 0)
        FROM total_order
        WHERE DATE(order_date) = CURDATE()
    </select>

    <select id="selectOrderStatusCount" resultType="map">
        SELECT order_status, COUNT(*) AS count
        FROM total_order
        WHERE DATE(order_date) = CURDATE()
        GROUP BY order_status
    </select>

    <select id="selectTotalRevenue" resultType="int">
        SELECT IFNULL(SUM(order_totalprice), 0)
        FROM total_order
        WHERE order_status IN ('결제완료', '배송중', '배송완료')
    </select>

</mapper>

